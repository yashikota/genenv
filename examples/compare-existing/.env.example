# Example for comparing with existing .env file
# This demonstrates how to add new fields to an existing .env file

# Existing fields (already in .env)
APP_NAME=My App
APP_ENV=development
APP_DEBUG=true

# New fields to be added
APP_KEY=${app_key}
APP_URL=${app_url}

# Database configuration
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=${db_database}
DB_USERNAME=${db_username}
DB_PASSWORD=${db_password}

# New feature configuration
FEATURE_FLAG=${feature_flag}
API_KEY=${api_key}
CACHE_DRIVER=${cache_driver}
