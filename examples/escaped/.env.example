# Example with escaped placeholders

# Normal placeholders that will be replaced
API_KEY=${api_key}
API_SECRET=${api_secret}

# Escaped placeholders that will be preserved as-is
TEMPLATE_EXAMPLE=This is a \${placeholder} that will not be replaced
ANOTHER_EXAMPLE=Another \${example} of escaped placeholder
MIXED_EXAMPLE=Replace this ${real_placeholder} but not this \${escaped_one}

# Example with dollar signs
DOLLAR_SIGN=$$
PRICE_EXAMPLE=$10.99

# Complex example with multiple escaped and non-escaped placeholders
COMPLEX_EXAMPLE=Replace ${first} and ${second} but not \${third} or \${fourth}

# Example with quotes
QUOTED_EXAMPLE="This is a ${quoted_placeholder} inside quotes"
ESCAPED_QUOTED="\${this_stays_as_is}"
